# DNSMon Environment Variables
# Copy this file to .env and customize as needed

# Postgres password (required)
POSTGRES_PASSWORD=changeme_secure_password

# Timezone for display (IANA format: America/Denver, Europe/London, etc.)
TZ=UTC

# Session security (optional but recommended for production)
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
# If not set, a random key is generated on each restart (invalidates sessions)
# DNSMON_SECRET_KEY=your-secret-key-here

# Set to "true" if running behind HTTPS reverse proxy
# DNSMON_COOKIE_SECURE=false

# Note: Pi-hole servers, notification channels, and other settings
# are configured via the Settings page in the web UI
